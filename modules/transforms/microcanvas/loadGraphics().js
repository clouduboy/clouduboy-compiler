// game.loadGraphics('sprite data [constant]');   >>>   pixeldata declarations;

/**
(`void`) Loads graphics into the canvas.

This is an alias for `@loadSprite()`, either forms can be used.
*/

const alias = require(`./loadSprite()`)

module.exports = (context) => alias(context)
