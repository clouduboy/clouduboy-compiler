// game.state   >>>   _microcanvas_state

/**
(`number`) Active game state.

This is/could be used for keeping track of the game states (via global
state machine helpers that are largely unimplemented at this point)

On the target this is accessed via the `_microcanvas_state global` variable
*/

module.exports = (context) => '_microcanvas_state'
